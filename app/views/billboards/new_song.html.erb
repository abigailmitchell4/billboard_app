<h1 class="ui header">Add A Song To: '<%= @billboard.name %>'</h1>	
<div class="container">
  
    <% @songs.each do |song| %>
      <%= link_to song.title, add_billboard_song_path(@billboard, song), method: :post %>
    <br/>
  <% end %>
  </div>

</br>
<%= link_to 'Back', billboards_path, class: 'ui button' %>